<UserControl x:Class="MKSS.APP.ECTester.UIZhuiSuC10LiveChart"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
            xmlns:local="clr-namespace:MKSS.APP.ECTester"
             mc:Ignorable="d" Background="{DynamicResource MaterialDesignPaper}" Foreground="LightGray"
             xmlns:lvc="clr-namespace:LiveChartsCore.SkiaSharpView.WPF;assembly=LiveChartsCore.SkiaSharpView.WPF"
             xmlns:ctx="clr-namespace:LiveChartsCore.Kernel;assembly=LiveChartsCore"
             d:DesignHeight="450" d:DesignWidth="1600">
    <UserControl.Resources> 
        <ContextMenu x:Key="ContextMenuA">
            <MenuItem Name="btnSelectAllA" Header="全　选" Click="btnSelectAll_Click"/>
            <MenuItem Name="btnUnSelectAllA" Header="全不选" Click="btnUnSelectAll_Click"/>
            <MenuItem Name="btnRevSelectAllA" Header="反　选" Click="btnRevSelectAll_Click"/>
            <Separator></Separator>
            <MenuItem Name="RefreshA" Header="刷新" Click="Refresh_Click"/>
        </ContextMenu> 
    </UserControl.Resources>
    <Grid  >
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="300*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="100*"/> 
        </Grid.RowDefinitions>

        <lvc:CartesianChart x:Name="plotter1" Margin="0,20,0,0" Background="{DynamicResource MaterialDesignPaper}" Foreground="Blue" ZoomMode="X" >
            <lvc:CartesianChart.TooltipTemplate>
                <DataTemplate>
                    <Border Background="#303030">
                        <ItemsControl  Background="{DynamicResource MaterialDesignPaper}" ItemsSource="{Binding Points, RelativeSource={RelativeSource AncestorType=lvc:DefaultTooltip}}"
                                       Width="650" Height="500" Padding="10,10,10,10" HorizontalAlignment="Right" >
                            <ItemsControl.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <WrapPanel HorizontalAlignment="Left" VerticalAlignment="Center" Orientation="Vertical" />
                                </ItemsPanelTemplate>
                            </ItemsControl.ItemsPanel>
                            <ItemsControl.ItemTemplate>
                                <DataTemplate DataType="{x:Type ctx:ChartPoint}">
                                    <Border Padding="0,4,0,0">
                                        <StackPanel Width="120" Margin="0,0,0,0" Orientation="Horizontal">
                                            <TextBlock Text="{Binding AsTooltipString}" Margin="0 0 8 0" Foreground="AntiqueWhite" />
                                            <lvc:MotionCanvas Margin="0 0 8 0" 
                                                PaintTasks="{Binding Context.Series.CanvasSchedule.PaintSchedules}"
                                                Width="{Binding Context.Series.CanvasSchedule.Width}"
                                                Height="{Binding Context.Series.CanvasSchedule.Height}"
                                                VerticalAlignment="Center"/>
                                        </StackPanel>
                                    </Border>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </Border>
                </DataTemplate>
            </lvc:CartesianChart.TooltipTemplate>
        </lvc:CartesianChart>
        <ItemsControl  Grid.Column="0" Grid.Row="0" Grid.RowSpan="2" Width="300" Padding="0,50,0,50" HorizontalAlignment="Right" 
              ToolTip="右键全选或反选" ContextMenu="{StaticResource ContextMenuA}"
             x:Name="legengA" Background="Transparent">
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <WrapPanel Orientation="Vertical"/>
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <Border Padding="0,4,0,0">
                        <StackPanel Width="70" Margin="0,0,0,0" Orientation="Horizontal">
                            <CheckBox IsChecked="{Binding Path=Checked, Mode=TwoWay}" Cursor="Hand" Tag="{Binding Path=Description}"  MouseLeave="TextPanel_MouseLeave"  MouseMove="TextPanel_MouseMove"/>
                            <Line Width="15" Height="15" X1="0" Y1="0" X2="15" Y2="15" Stroke="{Binding Path=Stroke}" Cursor="Hand" StrokeThickness="2" Tag="{Binding Path=Description}"  MouseLeave="TextPanel_MouseLeave"  MouseMove="TextPanel_MouseMove"/>
                            <TextBlock Margin="5,0,0,0" Foreground="LightGray" Text="{Binding Path=Description}"  Cursor="Hand" MouseEnter="TextPanel_MouseEnter" Tag="{Binding Path=Description}"  MouseLeave="TextPanel_MouseLeave"  MouseMove="TextPanel_MouseMove"/>
                        </StackPanel>
                    </Border>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>
        <StackPanel  x:Name="PAnelControl" Grid.Column="0" Grid.Row="0" Orientation="Horizontal"  Margin="45 -10 0 0" Height="50"  HorizontalAlignment="Left" VerticalAlignment="Top" >
            <RadioButton Style="{StaticResource MaterialDesignTabRadioButton}" x:Name="ShowModeDY" Margin="0" IsChecked="True" Content="端电压"  Foreground="Red" FontSize="19" />
            <RadioButton Style="{StaticResource MaterialDesignTabRadioButton}" x:Name="ShowModeAD" Margin="0" IsChecked="False" Content="AD值" FontSize="19"  />
            <CheckBox x:Name="LockX"  IsChecked="True" Visibility="Hidden" Margin="5 0 5 0"   Checked="LockX_Checked" Unchecked="LockX_Checked" Content="缩放电压" />
            <CheckBox x:Name="LockY" IsChecked="True"  Visibility="Hidden" Margin="5 0 35 0" Checked="LockX_Checked" Unchecked="LockX_Checked" Content="缩放时间" />
        </StackPanel>

    </Grid>
</UserControl>
