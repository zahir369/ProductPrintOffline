<UserControl x:Class="MKSS.APP.ElectroChemical.UIZhuiSuC10Chart"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
            xmlns:local="clr-namespace:MKSS.APP.ElectroChemical"
             mc:Ignorable="d" 
             xmlns:d3="clr-namespace:InteractiveDataDisplay.WPF;assembly=InteractiveDataDisplay.WPF"
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
        <local:VisibilityToCheckedConverter x:Key="VisibilityToCheckedConverter"/>
    </UserControl.Resources>
    <Grid >
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="160*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="10*"/>
        </Grid.RowDefinitions>
        <d3:Chart Name="plotter" 
                  IsXAxisReversed="False" PlotHeight="1050" PlotWidth="880" PlotOriginX="-10" PlotOriginY="-20"
                  PreviewMouseWheel="plotter_MouseWheel" MouseLeftButtonDown="plotter_MouseLeftButtonDown"
                  IsHorizontalNavigationEnabled="True" IsVerticalNavigationEnabled="True" >
            <d3:Chart.Title>
                <TextBlock HorizontalAlignment="Center" FontSize="18" Margin="0,5,0,5"> </TextBlock>
            </d3:Chart.Title>
            <d3:Chart.LegendContent>
                <d3:LegendItemsPanel>
                    <d3:LegendItemsPanel.Resources>
                        <DataTemplate x:Key="InteractiveDataDisplay.WPF.LineGraph">
                            <StackPanel Orientation="Horizontal">
                                <CheckBox IsChecked="{Binding Path=Visibility, Converter={StaticResource VisibilityToCheckedConverter}, Mode=TwoWay}"/>
                                <Line Width="15" Height="15" X1="0" Y1="0" X2="15" Y2="15" Stroke="{Binding Path=Stroke}" StrokeThickness="0.5"/>
                                <TextBlock Margin="5,0,0,0" Text="{Binding Path=Description}"/>
                            </StackPanel>
                        </DataTemplate>
                    </d3:LegendItemsPanel.Resources>
                </d3:LegendItemsPanel> 
            </d3:Chart.LegendContent>
            <Grid x:Name="lines"/>
        </d3:Chart>
        <CheckBox x:Name="LockX" IsChecked="False" Margin="0 10 20 0" HorizontalAlignment="Right" VerticalAlignment="Top" Checked="LockX_Checked" Unchecked="LockX_Checked" Content="缩放电压" />
        <CheckBox x:Name="LockY" IsChecked="True" Margin="0 10 110 0" HorizontalAlignment="Right" VerticalAlignment="Top" Checked="LockX_Checked" Unchecked="LockX_Checked" Content="缩放时间" />
    </Grid>
</UserControl>
