<UserControl x:Class="MKSS.APP.UIBiaoDing.UISheBeiBiaoDingCGK"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:MKSS.APP.UIBiaoDing"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
        <ResourceDictionary>
            <local:DataColorConverter x:Key="DataColorConverterStyle"/>
            <Style TargetType="DataGrid" >
                <Setter Property="BorderBrush" Value="#FFF5F7F5" />
                <Setter Property="HorizontalGridLinesBrush">
                    <Setter.Value>
                        <SolidColorBrush Color="#d6c79b"/>
                    </Setter.Value>
                </Setter>
                <Setter Property="VerticalGridLinesBrush">
                    <Setter.Value>
                        <SolidColorBrush Color="#d6c79b"/>
                    </Setter.Value>
                </Setter>
            </Style>
            <Style TargetType="DataGridColumnHeader" >
                <Setter Property="FontWeight" Value="Bold"/>
                <Setter Property="Background" Value="{DynamicResource UIBiaoDingBrush}"/>
                <Setter Property="Foreground" Value="#FF000000"/>
                <Setter Property="Height" Value="28"/>
            </Style>
            <Style x:Key="HyperlinkStyle" TargetType="TextBlock">
                <Setter Property="Foreground" Value="Blue" />
                <Setter Property="TextDecorations" Value="Underline" />
                <Setter Property="TextAlignment" Value="Center"/>
                <Setter Property="VerticalAlignment" Value="Center"/>
            </Style>
        </ResourceDictionary>
    </UserControl.Resources>
    <Grid >
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="120"/>
            <ColumnDefinition Width="100*"/>
            <ColumnDefinition Width="100*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="1"/>
            <RowDefinition Height="100*"/>
            <RowDefinition Height="35"/>
        </Grid.RowDefinitions>
        <Border  BorderBrush="{DynamicResource UIBiaoDingBrush}" 
                 Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="3" Grid.RowSpan="2" BorderThickness="10">

            <DataGrid x:Name="dataGrid" SelectionUnit="FullRow" SelectionMode="Single" SelectionChanged="dataGrid_SelectionChanged"
                      ItemsSource="{Binding}" IsReadOnly="True" AutoGenerateColumns="False"  RowHeaderWidth="0" >
                
                <DataGrid.CellStyle>
                    <Style TargetType = "{x:Type DataGridCell}" >
                        <Setter Property="Margin" Value="0"/>
                    </Style>
                </DataGrid.CellStyle>
                <DataGrid.Columns>
                    <DataGridTemplateColumn Header="编号" Width="100*" >
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <TextBlock HorizontalAlignment="Left" Style="{StaticResource HyperlinkStyle}" x:Name="OpenBatch"
                                      Margin="10,0,0,0"   Cursor="Hand"  Text="{Binding F_BatchId}" MouseLeftButtonUp="OpenBatch_MouseLeftButtonUp" />
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                    <DataGridTemplateColumn Header="产品型号" Width="260*" >
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <TextBlock HorizontalAlignment="Left" Style="{StaticResource HyperlinkStyle}" x:Name="OpenBatch"
                                      Margin="10,0,0,0"   Cursor="Hand"  Text="{Binding F_BatchName}" MouseLeftButtonUp="OpenBatch_MouseLeftButtonUp" />
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                    <DataGridTextColumn Header="地址" Width="100*" Binding="{Binding SensorGrougAddress}"/>
                    <DataGridTextColumn Header="总数（个）" Width="100*" Binding="{Binding StaTotal}"/>
                    <DataGridTextColumn Header="不良品（个）" Width="100*" Binding="{Binding StaUnqualified}"/>
                    <DataGridTextColumn Header="开始时间" Width="100*" Binding="{Binding F_AgingStartTimeStr}"/>
                    <DataGridTextColumn Header="结束时间" Width="100*" Binding="{Binding F_AgingEndTimeStr}"/>
                    <DataGridTemplateColumn x:Name="UserAction" Header="删除" Width="80">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <TextBlock HorizontalAlignment="Left" Style="{StaticResource HyperlinkStyle}" x:Name="BtnDelete"
                                      Margin="10,0,0,0"   Cursor="Hand" Foreground="Red" Text="删除" MouseLeftButtonUp="BtnDelete_Click" />
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>
        </Border>

    </Grid>
</UserControl>
